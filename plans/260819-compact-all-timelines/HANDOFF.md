# Handoff — 2026-08-19

Generated by: t1k:handoff save
Location: plans/260819-compact-all-timelines/HANDOFF.md

## Active Plan

- Path: plans/260819-compact-all-timelines/
- Current phase: not yet started — plan is written, spike-first per `state.md`
- Phase status: pending

## Progress

### Completed Tasks

- [x] PR #42 merged (squash `c0b85058d1`) — workload timeline: window-complete `periods` fix (removed phantom "120h" capacity denominator), per-week/viewport-following capacity badge, single time control, reference row layout, clickable work items via peek overlay, viewport-driven lazy loading with range caching/merging, removed dead "Over capacity only" control.
- [x] PR #43 merged (squash `1578e2f835`) — workload lane packing (non-overlapping tasks share a row), 3x wider timeline columns (one core edit, applied globally, documented in `docs/FORK.md`), workload app header/layout fix (sidebar collapse had no reopen affordance), dropdown-under-board stacking fix via `isolate`.
- [x] Both merged unreviewed via `gh pr merge --admin` at explicit user instruction — the "Default" ruleset requires 1 approving review on `company-main`, author can't self-approve, and repo auto-merge is disabled.
- [x] PR #44 opened — the plan doc for "compact all timelines" (docs only). **Still open, unmerged.**
- [x] Two live bugs found + fixed this session (see Key Decisions / evidence below).
- [x] 6 downstream propagation issues opened + amended: plane-mcp-server#14, plane-node-sdk#6, plane-python-sdk#6, plane-claude-plugin#4, docs#4, developer-docs#4.

### In Progress

- [ ] None — session ended between shipping PRs #42/#43 and starting the new plan's Phase 1.

### Blocked

- [ ] Plan `plans/260819-compact-all-timelines/` Phase 1 — blocked on a deliberate spike-first step (see `state.md` for why) before committing to phases 2-5.

## Key Decisions Made

- Cache + merge date ranges as the user pans the timeline, rather than always-refetching.
- Remove `weekly_buckets` / `weekly_capacity` from the API entirely rather than leave them as unused surface — this is why the 6 downstream propagation issues needed amending, not just filing.
- 3x column width applied via ONE core-file edit, global to all timelines (issue/cycle/module timelines widen too), rather than scoping the change to workload only via two core files. Documented in `docs/FORK.md`.
- Removed the built-in block-list sidebar from EVERY timeline, no opt-in flag. Workload keeps a sidebar, but it carries only avatar/name/capacity badge — not a work-item list.
- Per-block interactions (drag-to-reschedule, resize, dependency arrows, reorder, bulk-select) are DISABLED, not reworked. Restoration is deferred — see `later.md` in the prior plan dir.
- Admin-merge unreviewed PRs when CI is green (explicit user call, given the ruleset's self-approval deadlock).

### Two live bugs found and fixed this session

- Capacity badge denominator was computed over _populated_ buckets only, so it silently moved whenever unrelated members scheduled work. Fixed by making it window-complete.
- Default date window ran today..+12w (forward only) — any member whose scheduled work was in the past vanished entirely from the view. Reproduced live against the `unity` workspace: user `dovudieuanh`, 71 tasks / 216.5h, was returning `rows: []` before the fix.

## Verification Evidence

**Verified:**

- 165 backend tests pass.
- Web types / format / lint clean.
- `node packages/workload-ext/verify-merge.mjs` — 29 checks pass.
- Post-merge CI + self-hosted deploy green.
- `plane.the1studio.org` returns HTTP 200.
- Production API re-queried after deploy: confirms window-complete `periods`, `tasks[].project_id` present, no `weekly_*` fields remain, `dovudieuanh` now correctly returns 216.5h.
- Falsification passes (each reverted after confirming red): disabling the periods union → 3 backend tests fail; switching merge union→sum → idempotence check fails; relaxing lane-adjacency guard `<` → `<=` → 2 checks fail.

**NOT verified — no browser in this environment:**

- All visual/on-screen behavior: packed lane layout, 3x columns, app header, dropdown stacking, capacity badge rendering.
- Plane item PLANE-69 (Infrastructure ▸ Plane) is deliberately left OPEN for a manual browser pass — its 8 checks are recorded in the prior plan's `plans/260818-workload-timeline-fixes/phase-5.md`.

## Blockers

- None hard-blocking. PR #44 (the new plan doc) is open but unmerged — until merged, the plan is invisible to anyone else on `company-main`.

## Next Steps

1. Merge PR #44 so the compact-all-timelines plan is visible on `company-main`.
2. Do PLANE-69's 8 on-screen checks (browser pass) — likely higher value right now than starting new work, since nothing in PRs #42/#43 has been visually confirmed yet.
3. Spike Phase 1 of `plans/260819-compact-all-timelines/` before committing to phases 2-5 — see that plan's `state.md` for the specific reason a spike is required first, and `plan.md` / `phase-1.md`..`phase-5.md` for the decomposed work. `later.md` in the prior plan dir holds explicitly deferred per-block interaction restoration.

## Git State

- Branch: docs/plan-compact-all-timelines
- Last commit: 394af960e1 docs(workload): add handoff state so the plan survives a cold start
- Dirty files: `.claude/plane-binding.json` (untracked), `image.png` (untracked), `swappy-20260818_222623.png` (untracked)
- Uncommitted changes: none tracked; only the 3 untracked files above (session-local artifacts, not code changes)

## Session Notes

Repo: `/home/frostbun/Projects/plane` (The1Studio/plane fork). Integration branch for this fork is `company-main`, **not** `preview`. Plane board: root item PLANE-49 is Done; PLANE-69 is open for the manual browser verification pass. Repo is bound to Infrastructure ▸ Plane (project `2eae4e83-f715-4e4b-8753-cdc289bbe37f`) via `.claude/plane-binding.json`. The architectural invariants for the compact-all-timelines plan live in that plan's own `state.md` — refer to it rather than re-deriving here.
